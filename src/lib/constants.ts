export const AUTH_TOKEN_KEY = "auth_token";
export const AUTH_SESSION_KEY = "auth_session";

// Flip this to `false` when maintenance access restrictions are no longer needed.
export const MAINTENANCE_MODE_ENABLED = true;
export const MAINTENANCE_ALLOWED_EMAIL = "sabyasachim356@gmail.com";
